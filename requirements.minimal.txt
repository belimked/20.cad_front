# CAD Automation System - Dependencies (Minimal Version)
# Only includes packages that are actually used in the project
# Generated: 2025-10-26

# ========================================
# Core Dependencies (Required)
# ========================================

# Windows COM automation (AutoCAD)
pywin32>=305

# UI automation (for menu clicking)
pywinauto>=0.6.8

# Database ORM
sqlalchemy>=2.0.0

# MySQL database driver (required by src/utils/database.py)
pymysql>=1.1.0

# Configuration file parsing (required by src/utils/config.py)
pyyaml>=6.0

# Process monitoring
psutil>=5.9.0

# Type hints backport (for Python < 3.10)
typing-extensions>=4.5.0

# ========================================
# Development/Testing (Optional)
# ========================================

# Testing framework
pytest>=7.0.0
pytest-mock>=3.10.0
pytest-cov>=4.0.0
